# ICRA 实验设计与结果表格草稿

> 本文档用于确定实验分组、指标与表格版式。所有 `—` 均表示尚未填写的结果，不代表 0。配套脚本已实现，真实实验由操作员按 `n` 启动；本文不包含真实运行结果。

## 任务与统一评价规则

四个真机任务共用表格，方法放在行、任务放在列。正文逐任务展示成功率，Progress 和耗时展示跨任务汇总；逐任务、逐可见性条件的详细指标在补充材料中展开。

主要真机预算用于表 1 的完整 agent 实验。表 2 以三种方法验证近场初始化鲁棒性与双视角收益；副表 1、2 分别补充门控恢复与语义角色选择的证据。

| 编号 | 任务 | 操作 Progress 的物理子目标 | 最终成功条件 |
|---|---|---|---|
| T1 | 将药瓶放到篮子中 | 稳定抓起药瓶 → 放入篮中并释放 | 药瓶留在篮中，机器人完成释放 |
| T2 | 将垃圾袋放到箱子中，再将箱子搬下拖车 | 稳定抓起垃圾袋 → 放入箱中 → 稳定提起箱子 → 搬离拖车并放至指定区域 | 垃圾袋留在箱中，箱子离开拖车并稳定放置于指定区域 |
| T3 | 拿碗、踩开垃圾桶并倒垃圾 | 稳定拿起碗 → 用脚使桶盖达到可倾倒状态 → 将内容物倒入桶中 | 倾倒期间满足桶盖开启条件，内容物进入桶内 |
| T4 | 从床头柜顶取物，拉开抽屉并放入 | 稳定抓起物体 → 抽屉打开到可放置状态 → 放入物体并释放 | 物体留在抽屉内，机器人完成释放 |

- **SR（成功率）**：在预设时间和重试预算内，无人工干预地完成最终物理目标。表 1 的任务列填写 `k/16（百分比）`；正式论文同时报告置信区间。
- **Progress（操作任务进度）**：每次试验的有效子目标完成数除以该任务子目标总数。对必须顺序执行的子目标，按满足依赖关系的有效前缀计分；不额外要求任务未规定的顺序。每任务先求平均，再对四任务等权平均，以百分比报告。
- **物理判定**：接触不等于抓起，抓起需要脱离支撑并稳定保持；放入需要实际进入容器并释放。中途完成后又破坏最终结果的试验不能计为最终成功。具体保持时间、放置区域与倾倒完成标准在正式评测前固定。
- **Nav SR（导航到达率）**：到达正确目标的预定义可接近区域，且目标距离在 3 m 内。隔墙接近、错误实例或不可通行位置不算到达。Nav SR 单独记录，不作为一个同权子目标加入操作 Progress。
- **平均 SR**：每行方法在四个任务上的 SR 宏平均。表 1 中只有相同输入条件的方法直接比较；ObjectNav 能力展示行不参与 VLN 主比较的最优值排名，不跨输入条件取平均。
- **平均耗时†**：从按 `n` 的统一任务启动事件起计时，成功使用独立标注的实际完成时间，失败按该任务预设时限计时。先按任务求平均，再对四任务等权平均，单位为秒。当前默认总时限 **600 s**、VLA 操作上限 **180 s**，允许按任务统一调整；同一任务各方法的时限必须一致。成功但缺少物理完成时间时可计算 SR，耗时保持缺失，不使用 VA 宣告完成时刻代替。
- **独立评价**：最终成功和门控真值依据外部视频或独立标注判断，不直接使用系统自身 VA 的判断作为评价真值。

## 表 1：完整任务的对比与消融

### 评测条件

- 按下表列出的“方法 + 输入条件”逐行评测，每行每个任务使用 **16 个评测条件：8 个初始可见，8 个初始不可见**。主比较采用 VLN；ObjectNav 只运行完整系统，不重复整套消融。完整设计六行、四任务对应 384 次试验；本次脚本覆盖其中五行，共对应 320 次。UniLM 适配保留设计，暂不实现。
- 可见性基于统一的初始观测协议判定。用于支撑探索能力的“不可见”组要求目标在初始扫描中也不可见，需要移动搜索；仅正前方不可见、原地扫描可见的条件另行标注。
- 相同输入条件下，各方法使用配对的起点、场景和物体布局，轮换执行顺序。ObjectNav 行尽量复用相同物理条件，但使用目标搜索指令；VLN 行使用路线指令，两者分别解释。记录初始路径距离、可见性和布局编号作为难度描述，不把初始距离当作方法性能指标。
- 保持相同 VLA 权重、操作提示、VLA 相机输入、操作控制设置、任务时限与重试预算。训练和调参条件与最终评测条件分开。
- 去掉 BasePose 的版本在导航结束后按预先固定的规则直接启动 VLA；其他交接方法也使用明确的启动规则，避免因保留对 BasePose `aligned` 状态的依赖而无法执行。底层必要保护保持一致。
- **简单几何接近/居中**：头部 RGB-D + YOLOE，单个配置目标，沿用现有深度标定与跟踪。顺序执行转向目标中心（`wz=0.3 rad/s`）→ 沿 `vx=0.4 m/s` 接近 → 最后居中（`wz=0.2 rad/s`）。目标纵向距离默认 **1.1 m**、距离容差 **0.1 m**、角度容差 **9°**，连续 **3 个新观测**满足后结束。目标丢失时停止并等待重检测，过近时以相同速度后退纠偏；无侧移、结构边缘 yaw 或双角色控制。**60 s 超时记录对齐失败，停止底座后继续 VLA**，交接 VA 保留记录但不阻挡；最终物理任务成功独立标注。所有参数在开发条件下固定后写入公共 YAML。
- **UniLM-Nav adapted to our platform**：保留视角选择、任务相关 grounding、深度与机器人几何辅助的底座位姿推理，并接入相同 VLA。论文中说明与原方法的适配差异；仅让 LLM 输出移动动作不满足此方法定义。

### 完整任务结果

| 方法 | 输入条件 | T1 药瓶 SR ↑ | T2 搬箱 SR ↑ | T3 倒垃圾 SR ↑ | T4 抽屉 SR ↑ | 平均 SR ↑ | 平均 Progress ↑ | 平均耗时†（s）↓ |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Full agent | VLN | — | — | — | — | — | — | — |
| 去掉 BasePose，导航后直接启动 VLA | VLN | — | — | — | — | — | — | — |
| 导航替换为 NaVILA，保留 BasePose + VLA | VLN | — | — | — | — | — | — | — |
| BasePose 替换为简单几何接近/居中 | VLN | — | — | — | — | — | — | — |
| BasePose 替换为 **UniLM-Nav adapted to our platform** | VLN | — | — | — | — | — | — | — |
| Full agent（目标搜索能力展示） | ObjectNav | — | — | — | — | — | — | — |

- 去掉 BasePose、简单几何接近/居中和 UniLM-Nav 三行沿用 Full agent 的 VLN 导航模块及相同 VLA，仅改变交接方法。它们分别检验对齐整体收益、相对普通接近的收益，以及相对已有交接方法的收益。
- ObjectNav 单独一行用于展示自主搜索后完成任务的能力；该行不单独用于归因 BasePose 在 ObjectNav 下的性能提升。
- 逐任务另行记录可见组 SR（`k/8`）、不可见组 SR（`k/8`）、Nav SR、进入交接阶段的比例、VLA 启动率、启动 VLA 后的条件操作成功率，以及重试次数。**VLA 启动按首个有效动作实际下发统计**，请求和 ACK 另记；同一次执行的恢复不重复计数。失败后的重新导航/对齐计为恢复重试，正常探索移动不计为重试。条件操作成功率应与启动率同时报告。

## 表 2：3 m 内启动的初始化鲁棒性与相机消融

### 评测条件

- 使用固定的近场起点集合，同时包含接近训练标准状态的起点，以及存在距离、侧向位置、yaw 偏差的起点。分别记录距离、相对工作面的左右位置/方位角，以及机器人自身 yaw 偏差。具体分档、每档重复次数和每任务总试验数 `n` 在评测前固定。
- 三种方法使用相同物理起点、物体布局、VLA 权重、操作提示及总执行预算，不把导航搜索混入本表。纯 VLA 从给定起点直接执行；两个 BasePose 版本先对齐再执行相同 VLA。
- **单头相机**：对齐角色选择、感知、控制及 ALIGN → VLA 视觉检查均仅使用头部相机；VLA 仍使用原有四视角输入。归档的胸部图像和离线角色 mask 不参与控制。近场实验直接进入 ALIGN 或 VLA，导航阶段记为“不适用”。该行检验胸部视角对近场交接的补充作用。
- 简单几何与 UniLM-Nav 已在表 1 中进行完整流程比较，本表仅保留以下三行。标准状态附近的起点作为共同起点集合的一部分，用于观察各方法在良好初始化下的表现，不另设专家摆位参考行。

| 方法 | T1 药瓶 SR ↑ | T2 搬箱 SR ↑ | T3 倒垃圾 SR ↑ | T4 抽屉 SR ↑ | 平均 SR ↑ | 训练邻域覆盖率‡ ↑ |
|---|---:|---:|---:|---:|---:|---:|
| BasePose + VLA（完整方法，头部 + 胸部相机对齐） | — | — | — | — | — | 未计算 |
| BasePose + VLA（仅头部相机对齐） | — | — | — | — | — | 未计算 |
| 纯 VLA（相同近场起点） | — | — | — | — | — | 未计算 |

各任务 SR 填写 `k/n（百分比）`，同一任务的三行使用相同试验数和配对条件。完整方法与纯 VLA 的对比验证组合系统的初始化鲁棒性；完整方法与单头相机的对比验证双视角对近场交接可靠性的收益，不单独据此估计双视角对全流程 SR 的提升。

### 训练起始分布重合度的操作定义

**本阶段暂不计算训练邻域覆盖率。** 先保存每次首次真正送入 VLA 策略的四视角 RGB、匹配的头/胸深度及单位/来源、内外参、时间戳与匹配误差、可获得的机器人位姿/朝向/坐标系、两个角色及各自的 mask。mask 在试验后基于保存图像生成；无对齐角色时使用 `vla_prompt` 离线选择并注明来源。深度不同步、位姿缺失或角色不可见时记录缺失原因，不用后续帧补充首帧。以下定义保留为后续位姿分析方案。

- 将训练示范起始位姿与实际 VLA 启动前位姿变换到统一的对象/工作面坐标系，分别记录平移与 yaw。每条示范按预先固定的起始帧规则取样。
- 在开发数据上固定平移、yaw 的归一化尺度和邻域阈值，使用到最近示范起始位姿的归一化距离判断是否落入训练起始位姿邻域。yaw 差采用环绕角差，不能直接相减跨越 ±180° 的角度。
- **训练邻域覆盖率‡**：已启动 VLA 且启动位姿落入预定义训练邻域的试验数，除以全部近场试验数。先逐任务计算，再对四任务等权平均。
- 未启动、对齐超时或缺失有效位姿的试验不计入分子，并分别记录数量；不只统计成功交接的样本。另行报告 VLA 启动率、有效位姿比例及已启动样本的位姿误差分布，以区分覆盖率下降的原因。
- 此指标仅衡量几何邻近程度，不能单独证明完整观测分布一致或操作一定成功。最终结论以相同 VLA 的真实操作成功率为准。

### 配套图（待填数据后绘制）

1. **起始条件与成功率图**：四个任务分别展示表 2 三种方法在距离、方位和 yaw 偏差下的成功率，方法间共用起点和颜色尺度。
2. **交接位姿图**：叠加训练示范起始位姿、导航结束位姿和 VLA 启动前位姿，用颜色区分后续操作成功/失败。包含表 1 的真实导航终点分析。
3. **几何误差与操作成功关系图**：展示位姿偏差减少是否伴随成功率提升，并交代未启动或缺少位姿的试验。

## 副表 1：门控有效性与扰动恢复

对每类门控分别设置无扰动组与固定扰动组，在相同条件下比较开启和去掉该门控的版本；其余门控和必要底层保护保持一致。每组样本数、扰动幅度、触发事件和恢复预算在评测前固定。

| 门控 | 对照设置 | 扰动示例 | 无扰动错误阻挡率 ↓ | 扰动错误放行率 ↓ | 扰动后完整任务 SR ↑ | 恢复耗时§（s）↓ |
|---|---|---|---:|---:|---:|---:|
| 导航 → 对齐的目标与距离就绪门控 | 开启 | 导航结束、检查前移动目标或改变其可见性 | — | — | — | — |
| 导航 → 对齐的目标与距离就绪门控 | 去掉该门控 | 与开启组相同 | — | — | — | — |
| 对齐 → 操作的几何与视觉就绪门控 | 开启 | 对齐阶段或停止后检查前移动目标、遮挡所选角色 | — | — | — | — |
| 对齐 → 操作的几何与视觉就绪门控 | 去掉该门控 | 与开启组相同 | — | — | — | — |
| 操作完成的视觉检查门控 | 开启 | 设置外观接近完成但物理目标尚未满足的状态 | — | — | — | — |
| 操作完成的视觉检查门控 | 去掉该门控 | 与开启组相同 | — | — | — | — |

- **错误放行率**：真实条件不满足时仍放行的检查事件数 / 真实条件不满足的检查事件总数。对完成门控，放行指提前宣告任务完成。
- **错误阻挡率**：真实条件满足时仍阻挡的检查事件数 / 真实条件满足的检查事件总数。检查事件与允许响应时间在评测前定义，不能按视频帧反复计数。
- **扰动后完整任务 SR**：发生预定义扰动的试验中，在统一预算内完成最终物理任务的比例；提前宣告完成不能计为成功。
- **恢复耗时§**：从扰动触发到完整任务成功；未恢复成功按预设恢复预算计时。无有效分母的指标填“不适用”，不填 0。
- 去掉门控后的继续执行或终止规则必须预先固定，不能因缺失门控输出而让对照停滞。按固定事件触发扰动，使用外部视频或独立标注判断真值。
- 当前关闭规则：NAV → ALIGN 在最终目标导航产生结束候选后放行；ALIGN → VLA 在对齐控制结束后放行，不由几何就绪或视觉结果阻挡；完成门控关闭时，VLA 从首个有效动作起固定执行默认 **180 s** 后停止，记录“完成候选”，不自动标注物理成功。全部组保留 VA 建议及实际执行决定，迟到结果只记录，不能延长固定操作时长或影响新试验。底层保护、取消和独立总时限始终生效。
- 扰动组到达目标阶段后提示并等待操作员施加扰动、输入 `:perturb` 确认，记录实际标记时间；等待仍计入总时限。预定义扰动与额外接管分别记录。没有实际扰动确认的试验不进入扰动恢复分母。
- 汇总只统计当前被消融的门控。缺少独立真值填“待标注”；已有真值但无适用分母填“不适用”。不直接将系统 VA 输出作为门控真值。
- 表中先展示三类阶段门控；如果需要进一步定位 ALIGN 门控收益，可在补充实验中分别去掉几何检查和视觉检查。

## 副表 2：VA 语义角色选择的正确性与可用性

用固定的“操作指令 + 图像”集合评价自动选择的位置角色与 yaw 角色。独立标注每个样本的可接受角色集合，允许多个合理答案，不要求目标名称与单一标注字符串完全相同。

两个角色允许使用同一物体或不同物体，由任务和可见结构决定。本组通过人工标注评价自动选择的正确性与可用性，不安排人工指定角色后的真机执行对照，也不单独消融强制单物体对齐。

| 任务 | 样本数 | 位置角色正确率 ↑ | yaw 角色正确率 ↑ | 双角色联合正确率 ↑ | 检测可用率 ↑ |
|---|---:|---:|---:|---:|---:|
| T1 药瓶入篮 | — | — | — | — | — |
| T2 垃圾袋入箱并搬箱 | — | — | — | — | — |
| T3 踩桶并倒垃圾 | — | — | — | — | — |
| T4 柜顶取物并放入抽屉 | — | — | — | — | — |
| 四任务宏平均 | 不适用 | — | — | — | — |

- **位置角色正确率**：选择了可接受的位置目标及正确物体实例的样本比例。
- **yaw 角色正确率**：选择了可接受的朝向参考，且该参考具有适用的可见结构的样本比例。
- **双角色联合正确率**：所选角色对整体可接受的样本比例；两个角色分别合理但组合不适用时不计正确。
- **检测可用率**：自动选择的角色对能被固定检测/跟踪流程正确定位，且提供控制所需几何观测的样本比例。该指标与语义正确率分开报告。
- 配套展示正确选择、多个合理答案、错误实例、缺少有效边缘等案例图。本组结果支持语义角色选择的准确性与可用性；对完整交接方法的最终操作收益，以表 1、2 的执行结果为依据，不将角色标注准确率解释为人工纠正角色后的成功率增益。

## 正式评测前需要固定的项目

- 表 1 的任务时限、重试预算、初始观测协议及配对条件清单。
- 表 2 的标准状态与偏移起点分档、每档重复次数及每任务总试验数；单相机版本固定使用头部相机。
- 训练位姿邻域的坐标系、归一化尺度、阈值和缺失位姿处理。
- 门控扰动协议、无门控版本的阶段切换规则及恢复预算。
- 物理子目标的具体判定阈值、独立标注方式与置信区间报告方式。

## 实验脚本与记录入口

启动与标注说明见 [实验脚本 README](gear_sonic/scripts/experiments/README.md)。15 对入口/YAML 分别位于 `gear_sonic/scripts/experiments/` 和 `gear_sonic/config/experiments/`；公共任务定义在 `tasks.yaml`，条件模板按四张表分组。

```bash
bash gear_sonic/scripts/experiments/full_vln.sh --task T1 --case visible_01 --dry-run
```

先填写真实权重标识、训练提示词和路线/物理布局。`vla_prompt` 描述真实任务，供 LA/VA 使用；`vla_trained_prompt` 是原样提供给实际策略的训练字符串，两者同时记录。一次启动选择一个方法、任务、条件，按 `n` 新建试验编号；再次按 `n` 不覆盖旧结果，不自动执行下一条件。

每次启动使用一个 `events.jsonl` 关联所有试验；仅保存统计所需的阶段/检查/交接/扰动/结果事件及必要图像与首帧资料。人工成功、Progress、物理完成时刻、Nav SR、门控真值、角色正确性追加到同一日志。汇总输出逐试验 CSV、统计 JSON 和四张 Markdown 表，包含分子/分母、样本量、缺失标注数及置信区间。默认不归档逐帧控制调试、完整模型请求或连续视频。
